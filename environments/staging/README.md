# Staging environment

This is the orchestration root for staging-specific infrastructure not already owned by a provider-native Git integration root.

Reusable modules may be composed here when staging owns their lifecycle. Provider-native roots remain authoritative for resources managed by Neon, Supabase, Cloudflare, or similar Git integrations.
