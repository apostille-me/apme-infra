# Preview environment

This is the orchestration root for infrastructure that is preview-specific and not already owned by a provider-native Git integration root.

Do not duplicate Neon/Supabase/Cloudflare resources here. Compose reusable modules here only when preview owns the resource lifecycle.
