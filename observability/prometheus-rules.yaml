groups:
  - name: apme.availability
    rules:
      - alert: APMEServiceUnavailable
        expr: up{namespace=~"apme.*"} == 0
        for: 5m
        labels: { severity: page }
        annotations:
          summary: "Apostille Me service unavailable"
