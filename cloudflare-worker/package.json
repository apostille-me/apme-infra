{
  "name": "@apostille-me/apme-edge-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Edge intake, signed status webhooks, document events, and customer notification fan-out.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "node --check src/index.mjs && node --check src/security.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
